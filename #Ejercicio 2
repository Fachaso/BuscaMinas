#Ejercicio 2:
def calcular_numeros(tablero):
    for i in range(len(tablero)):
        for j in range(len(tablero[0])):
            if tablero[i][j] == 0:
                # Contar cuántos -1 hay alrededor
                minas_cerca = 0
                for x in range(i - 1, i + 2):
                    for y in range(j - 1, j + 2):
                        if 0 <= x < len(tablero) and 0 <= y < len(tablero[0]):
                            if tablero[x][y] == -1:
                                minas_cerca += 1
                tablero[i][j] = minas_cerca
    # Los -1 quedan igual automáticamente

# Ejemplo
"""
tablero = [
    [-1, -1, 0],
    [0,  0, 0],
    [-1, 0, 0]
]

calcular_numeros(tablero)

for fila in tablero:
    print(fila)
"""
#fin del ejemplo
